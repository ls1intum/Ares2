<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
		"-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
		"https://checkstyle.org/dtds/configuration_1_3.dtd">

<!--
	Ares checkstyle rules.

	Formatting is owned by spotless and the Eclipse formatter (eclipse-formatter-rules.xml,
	eclipse-rules.importorder in this directory). Checkstyle deliberately does not repeat it,
	so that the two tools cannot contradict each other. google_checks.xml, used before, did
	contradict it: 95.8% of its 42,832 findings were FileTabCharacter, Indentation, LineLength
	and CustomImportOrder, i.e. it demanded two-space indentation and 100 columns from a
	code base the formatter writes with tabs at 120 columns. Those are not defects.

	What remains here are checks for defects and for conventions the formatter cannot express.
-->
<module name="Checker">
	<property name="severity" value="error"/>
	<property name="fileExtensions" value="java"/>

	<module name="TreeWalker">

		<!-- Imports -->
		<module name="UnusedImports"/>
		<module name="RedundantImport"/>
		<module name="IllegalImport"/>

		<!-- Correctness -->
		<module name="EqualsHashCode"/>
		<module name="CovariantEquals"/>
		<module name="EqualsAvoidNull"/>
		<module name="StringLiteralEquality"/>
		<module name="FallThrough"/>
		<module name="MissingSwitchDefault"/>
		<module name="DefaultComesLast"/>
		<module name="EmptyStatement"/>
		<module name="SimplifyBooleanExpression"/>
		<module name="SimplifyBooleanReturn"/>
		<module name="NoFinalizer"/>
		<module name="IllegalInstantiation"/>
		<module name="MissingOverride"/>

		<!--
			NeedBraces is kept deliberately. A brace-less single-statement if is the shape
			behind the "goto fail" class of bug, where a later edit silently lands outside
			the branch. Ares is a security boundary, so the cost of the rule is worth paying.
		-->
		<module name="NeedBraces"/>

		<!-- Clarity -->
		<module name="EmptyBlock">
			<property name="option" value="text"/>
		</module>
		<module name="OneStatementPerLine"/>
		<module name="MultipleVariableDeclarations"/>
		<module name="ModifierOrder"/>
		<module name="RedundantModifier"/>
		<module name="ArrayTypeStyle"/>
		<module name="UpperEll"/>
		<module name="FinalClass"/>
		<module name="HideUtilityClassConstructor"/>
		<module name="InterfaceIsType"/>
		<module name="PackageDeclaration"/>

		<!-- Naming -->
		<module name="TypeName"/>
		<module name="MethodName"/>
		<module name="ParameterName"/>
		<module name="LocalVariableName"/>
		<module name="ConstantName"/>
		<module name="PackageName">
			<property name="format" value="^[a-z]+(\.[a-zA-Z_][a-zA-Z0-9_]*)*$"/>
		</module>
	</module>
</module>
